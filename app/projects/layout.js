export const metadata = {
  title: "Rashed Abdullah - Projects",
  description:
    "Rashed Abdullah's Portfolio Website, Project page, there is all of projects of my github",
};

const ProjectLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default ProjectLayout;
